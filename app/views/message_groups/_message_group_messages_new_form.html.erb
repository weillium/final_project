<%= form_with(model: @message, local: true) do |f| %>

  <div class="form-group">
    <%= f.label :sender_id %>

    <%= f.collection_select :sender_id, User.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <%= f.hidden_field :receiver_id, value: @message_group.id %>

  <div class="form-group">
    <%= f.label :body %>
    <%= f.text_area :body, rows: "3", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :attachment %>

    <%= f.text_field :attachment, class: "form-control" %>
  </div>

  <%= f.submit "Create message", class: "btn btn-block btn-outline-secondary" %>
<% end %>
